function calculateNumber (var1, var2) {
  // Example implementation: round both values and return their sum.
  const rounded1 = Math.round(var1);
  const rounded2 = Math.round(var2);
  return rounded1 + rounded2;
}